import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.view.child',
  appName: 'cs-view-child',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
